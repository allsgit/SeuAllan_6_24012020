    const mimikeel = new Photographe (
      data.photographers[0].city,
      data.photographers[0].country,
      data.photographers[0].id,
      data.photographers[0].name,
      data.photographers[0].portrait,
      data.photographers[0].price,
      data.photographers[0].tagline
    );

    console.log (mimikeel);

    const photographerCity = document.querySelector ('.photographer-location');
    const photographerTag = document.querySelector ('.photographer-tag');
    const photographerPrice = document.querySelector ('.photographer-price');

    photographerCity.innerText = `${mimikeel.city} , ${mimikeel.country}`;
    photographerTag.innerText = mimikeel.tagline;
    photographerPrice.innerText = `${mimikeel.price} € / jours`;